# modules/ci

WS-09-owned build, signing-workflow, and release-evidence code. This module has
no product request path, policy-decision semantics, signing-key custody, or
runtime activation authority.

The Phase 1 policy activation/release builder is in [`policyrelease/`](policyrelease/).
