# Search contract

The authoritative Phase 0 model is [search-resource-model.md](./search-resource-model.md). Search supports multiple canonical resource types and performs partitioning plus authoritative policy checks before returning any item, title, snippet, count, facet, suggestion, rollup, activity hint, notification hint, or relationship summary.
