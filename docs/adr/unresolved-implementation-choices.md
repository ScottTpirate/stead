# Unresolved implementation choices requiring ADRs

**Status:** Reconciled candidate queue; all entries deferred to their named decision point<br>
**Rule:** An ADR may select among conforming options; it may not silently change a locked decision.

## Admission test

A choice belongs here only when it is unresolved by the directive and has durable effects on a public/cross-module contract, security boundary, source of truth, operational topology, compatibility, portability, or difficult-to-reverse data representation. Library versions, local refactors, test-tool selection, and other reversible implementation details remain ordinary issues subject to license review.

Each accepted ADR must state requirement IDs, options including status quo, decision drivers, security/classification and privacy effects, affected contracts/owners, standards mapping, data migration/backward compatibility, upgrade/rollback/recovery, test and observability changes, dependency/license evidence, and a supersession plan. Locked-decision changes additionally require explicit project-owner approval.

## Deferred choices required before dependent implementation

Phase 0 fixes each candidate's non-negotiable boundary but does not need to select its physical implementation. None is a conflict introduced by the v0.2 reconciliation. Each remains `DEFERRED` and blocks the named dependent implementation—not the Phase 0 owner review—until an ADR is accepted.

| Candidate | Decision genuinely left open | Non-negotiable constraints | Owner / reviewers | Blocks |
|---|---|---|---|---|
| `ADR-CAND-001` Canonical URI and compatibility profile | Exact provider-independent URI grammar, tenant scoping, resource-kind encoding, canonical URL versus URI relationship, schema-version negotiation, and deprecation window | UUIDv7 remains the canonical ID; links survive provider migration; OWGP/OSLC/PROV mapping; no provider locator leaks | WS-01 / WS-02,03,08,11,13 | OWGP v0.1, schemas, OpenAPI, redirects |
| `ADR-CAND-002` PostgreSQL module isolation and cross-module reads | Database-versus-schema layout, table namespace conventions, read-only contract views/API use, transaction coordination, and migration ordering inside the modular monolith | PostgreSQL authoritative; one owner per table/migration; no direct cross-module writes; Gitea/OpenFGA boundaries; projections rebuildable | WS-01 / WS-02,07,12,13 | Database ownership map, outbox, backup/upgrade |
| `ADR-CAND-003` Authorization service topology | In-process façade versus separately addressable internal component, OpenFGA/OPA call sequence realization, fail-closed timeouts, decision cache keys/TTL, invalidation, provider credential issuance, and representation of delegation/task scope/independent agent revocation | Central combined OpenFGA+OPA decision path; `user`/`agent`/`service_account`; no broad human-to-agent inheritance; future delegator/agent/task/runtime/session/resource intersection; no module alternative; no admin bypass; provider-path enforcement; auditable model/bundle versions | WS-06 / WS-01,02,03,07,08,13 | API security contract, direct-provider controls, agent-ready seams, golden tests |
| `ADR-CAND-004` Security-label algebra and profile identifiers | Formal partial order/join representation, incomparable compartments/releasability/handling composition, commercial profile vocabulary, stable IDs, and schema/profile version compatibility | Directive fields and US values; CUI is handling, not a level; derived content never weakens; ceiling/cross-domain deny; signed profiles; two-person downgrade where required | WS-06 / WS-01,04,08,09,10,12,13 | Label schema, OPA/OpenFGA inputs, every data contract |
| `ADR-CAND-005` Trusted principal and runtime-attribute normalization | Canonical human/principal claim representation, multiple-authority precedence, freshness/revocation, SCIM/OIDC correlation, conflict behavior, and future agent runtime/domain/ceiling/compartment/model-provider/tool-scope/environment attributes | `PrincipalRef` includes user/agent/service_account/directory_group; only the first three act; users/agents cannot self-assert trusted authority; provenance/version/issue/review or expiry/sync result required; unverifiable/expired fails closed; external agent runtimes remain possible | WS-06 / WS-01,02,08,12,13 | OPA contract, principal/identity API/schema, provisioning and seam tests |
| `ADR-CAND-006` Event tenant/domain partition and replay contract | NATS subject/account/stream partitioning, resource ordering key, retention classes, dead-letter naming, replay authorization, event payload minimization, and schema compatibility enforcement | JetStream from first slice; CloudEvents/AsyncAPI; transactional outbox; at-least-once/idempotent; no global-order assumption; protected subscriber isolation | WS-07 / WS-01,02,06,08,12,13 | AsyncAPI, outbox, projections, air-gap/security tests |
| `ADR-CAND-007` Policy-bundle distribution and trust roots | OPA bundle build/distribution, signing format and trust-root rotation, offline verification, activation/rollback, and coordinated OpenFGA model migration | Versioned, signed, tested, auditable bundles; no unapproved network dependency; fail closed; government air-gap support | WS-06 / WS-01,07,12,13 | OPA contract, install/upgrade, evidence chain |
| `ADR-CAND-008` Provider reconciliation conflict semantics | Per-capability source-of-truth precedence, webhook/version/idempotency tokens, scheduled full reconciliation, direct-change accept versus reset rules, and degraded-provider behavior | Stock supported Gitea APIs only; fixed SCM-003 mapping; canonical model cannot be expanded by provider values; violations produce clear audit | WS-03 / WS-01,02,06,07,13 | Provider interfaces, tracker workflow, golden scenario |

If an approved Phase 0 contract resolves one of these choices directly and records the complete rationale/effects, the architecture owner may convert the candidate into that contract's decision record rather than duplicate it. The decision must still be immutable, reviewable, and linked from this queue.

## Deferred ADRs with explicit decision points

These decisions are real but do not authorize Phase 0 to design feature depth prematurely.

| Candidate | Decide no later than | Decision and trigger | Locked constraints |
|---|---|---|---|
| `ADR-CAND-009` Document collaboration persistence | Before Phase 2 collaboration implementation | Yjs update/snapshot persistence, compaction, branch/PR interaction, reconnect/conflict recovery, and deterministic Markdown projection | TipTap Markdown editor; Yjs before 1.0; Git/Markdown/OKF system of record; no edit loss; repository security boundary |
| `ADR-CAND-010` Commonplace fallback | Beta integration checkpoint only if upstream/headless path cannot meet unified UX | Continue removable patch queue or activate native docs UI fallback, with objective compatibility evidence | Upstream-first Gitea/auth/headless/token hooks; no iframe primary UX; no permanent fork or divergent data model; project-owner-approved ADR required for fallback |
| `ADR-CAND-011` Search domain isolation and semantic profile | Before Phase 2 scale/government search | PostgreSQL index partitioning, optional vector model/data flow, OpenSearch logical/physical separation, rebuild/cutover and leakage-resistant aggregation | Search is projection; SearchProvider; authoritative OpenFGA+OPA filtering; no leaked totals/facets/snippets/suggestions; no required external service |
| `ADR-CAND-012` Blob delivery and partition topology | Before first non-filesystem BlobStore implementation | Gateway-streamed versus scoped short-lived URLs, provider partition keys/buckets/accounts, malware-scan quarantine, encryption metadata, and locator migration | Provider-independent model; platform authorization/policy-controlled URL; organization/domain/classification partitioning; no locator exposed; portable manifests |
| `ADR-CAND-013` Runner isolation and trust tiers | Before Phase 2 runner pools | Supported isolation engine(s), ephemeral cleanup proof, cache/network boundaries, privileged exception mechanism, workload credentials, and classification-pool admission | Gitea Actions; ephemeral by default; secure profiles deny egress; separate pools; no lower-to-higher access; approved pinned actions |
| `ADR-CAND-014` Audit tamper-evidence scheme | Before Phase 3 tamper-evident audit implementation | Partition/hash-chain structure, checkpoint signer/trust rotation, external anchoring options, retention and verification/export format | Append-only required fields; no unnecessary content/secrets; offline/air-gap and SIEM/object/syslog exports; no required cloud |
| `ADR-CAND-015` FIPS-capable deployment cryptographic boundary | Before government-airgap production profile | Exact validated modules/providers, TLS/key/storage integration boundaries, approved-mode detection, evidence, fail/rollback behavior | Support FIPS 140-3-validated modules/algorithms without claiming validation for an unvalidated build; no mandatory cloud; offline capable |
| `ADR-CAND-016` Migration identity/collision and redirect model | Before Phase 2 importer writes canonical data | Principal correlation, duplicate/collision rules, preserved authorship, immutable source keys, canonical redirect precedence, delta/cutover conflict policy | Resumable/idempotent staged subsystem; preserve listed history/provenance; unsupported constructs explicit; canonical ontology unchanged |
| `ADR-CAND-017` Agent interoperability profile | Before implementing the first executable agent integration | MCP tool/resource surface and versioning, external-runtime registration, A2A/Agent Card profile, delegation handoff, credential lifetime, cancellation/revocation, and compatibility lifecycle | Platform APIs and platform-wide MCP for canonical business access; scoped direct Git only; no provider-specific business API; no required model, SDK, runtime, orchestrator, or provider; Phase 0 preserves seams but implements none of this execution surface |
| `ADR-CAND-018` Controlled typed properties | Before any Phase 2 local-field implementation | Whether to permit the bounded text/number/date/fixed-select/User/Agent/Team/resource-reference property set, its storage/index/export representation, limits, and compatibility | No scripts, custom validation/workflow, authorization/classification use, mandatory-field fragmentation, primary navigation, or new ontology; Phase 1 has no user-defined fields |
| `ADR-CAND-019` Recurring work and automation | Before any scheduler or event-rule implementation | Schedule/rule identity, idempotency, authority, execution limits, failure/retry, audit, and migration model | Rules act only through Platform APIs/events on canonical resources; no arbitrary workflow scripting engine; security/classification and initiating/acting principal context remain mandatory |
| `ADR-CAND-020` External guest principals | Before any external/guest collaboration | Principal representation, invitation/federation, visible marking, scope, independent revocation, lifecycle, audit, export and classified-profile defaults | No implicit organization membership or human-role inheritance; disabled by default for classified/government profiles unless explicitly approved; no provider-specific guest identity leaks into canonical contracts |
| `ADR-CAND-021` Initial Team relation model | Before `STEAD-P1-006` authorization implementation | Whether `lead`, `member`, and `contributor` are distinct OpenFGA relations or stable Platform role labels mapped to a smaller explicit relation set; cardinality, Directory Group participation, provisioning, revocation, reparenting, API/UX labels, migration, and rollback | Team hierarchy and owning/contributing Project accountability never grant access; no implicit organization visibility; Directory Groups never act; OPA only restricts an explicit relationship allow; no arbitrary role or configurable-ontology engine |

## Choices that do not currently require an ADR

- Naming the repository/project **Stead** while retaining directive-defined `platform-*` component contract names.
- Choosing a formatter, linter, test runner, or patch version that stays inside approved contracts and license policy.
- Adding a test fixture, diagnostic message, or internal refactor that changes no contract, policy result, data representation, or boundary.
- Organization display labels, branding, tags, notification settings, and approved provider configuration already permitted by PRIN-003 and UX-005.
- Selecting between directive-permitted deployment adapters for a particular installation; changing required supported profiles or making one cloud mandatory would require an ADR and project-owner approval.
- Reconciling the v0.2 open-work requirements with the previously approved agent-ready requirements as recorded in `PHASE0_RECONCILIATION_REPORT.md`; no locked decisions conflict.
- Using the stable discriminator `service_account` for the canonical Service Principal entity while documenting `service_principal` as the v0.2 source term; this is a compatibility-preserving name mapping, not a second entity.

## Explicitly not reopened

No candidate in this list invites reconsideration of stock Gitea, the Devlane primary frontend, Commonplace non-forking, language choices, PostgreSQL, NATS, transactional outbox, OpenFGA, OPA, Git/Markdown/OKF, fixed ontology/workflow, tracker repositories, container-level classification, required standards, deployment portability, cross-domain prohibition, open-source essential security, Apache-2.0 default, or the prohibited dependency classes.
